type RoutePages = {
  complexes: string;
  advertisements: string;
  us: string;
  contact: string;
  immovables: string;
  users: string;
  registers: string;
  auth: string;
  profile: string;
  registerProperty: string;
};

export type { RoutePages };
